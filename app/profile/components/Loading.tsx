import { Atom, OrbitProgress } from 'react-loading-indicators';
/* 
| OR directly pull it 😎
| import Atom from "react-loading-indicators/Atom";
*/

const Loading = () => <OrbitProgress color='#32cd32' size='small' text='' textColor='' />;

export default Loading;
